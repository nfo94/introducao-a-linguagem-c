#include <stdio.h>
#include <stdlib.h>

/*  */

int main() {
	float n, contador = 0;
	scanf("%f", &n);
	while (contador < n){
		printf("Eu amo programar\n");
		contador = contador + 0.5;
	}
	
	return 0;
}
