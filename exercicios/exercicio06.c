#include <stdio.h>
#include <stdlib.h>


int main() {

    float base, altura, area;
    area = base * altura;
    scanf("Type the numbers to calculate: %f%f", &base, &altura);
    printf("The area is: %f", area);

    return (0);

}

