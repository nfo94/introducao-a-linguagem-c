#include <stdio.h>
#include <stdlib.h>


int main(){

    float a, b, c;
    scanf("%f", &a);
    b = a - 500;
    c = b * 0.5;

        if (a > 500){
            printf("You'll pay %f in taxes.", c);
            }

    return 0;
}
